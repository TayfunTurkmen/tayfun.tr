/** @type {import('next').NextConfig} */
const nextConfig = {
module.exports = {
  images: {
    localPatterns: [
      {
        pathname: '/',
        search: '',
      },
    ],
  },
}
};

export default nextConfig;
